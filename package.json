{
  "name": "perimedi",
  "private": true,
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "test": "npm --prefix web test",
    "preview": "npm --prefix web run preview",
    "lint": "npm --prefix web run lint",
    "shot:se": "npm --prefix web run shot:se",
    "shot:journey": "npm --prefix web run shot:journey",
    "shot:responsive": "npm --prefix web run shot:responsive"
  }
}
